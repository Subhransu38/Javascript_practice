// Join()
// Array to string

const arr = [1, 2, 3, "4", 5, "6", true, 7, 8, "Subhransu", 6, 7];

let str = arr.join(".");
console.log("array to string: ", str);

// syntax

// arr.join("separator"); if the separator string is not given then the default is (, comma)
